import { Map } from "../Map";

export const ContactLanding = () => {
  return (
    <section className="bg-dark relative flex items-center tablet:justify-evenly">
      <Map variant="hero" />
      <div></div>
    </section>
  );
};
